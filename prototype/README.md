# Prototype (Tutorial Build)

This folder contains the original learning prototype built while exploring HTML5 Canvas runner fundamentals
(game loop, sprite animation, basic spawning/collision).

It is kept for reference. The production version of the game is in `/src`.